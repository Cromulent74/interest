/* Action types */



/* Actions creators */



/* Initial state */

const initialState = {
    notifications: false
};

/* Reducer */

export const headerReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

