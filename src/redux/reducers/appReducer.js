/* Action types */



/* Action creators */



/* Initial state */

const initialState = {
    initialized: false
};

/* Reducer */

export const appReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

/* Thunk creator */

